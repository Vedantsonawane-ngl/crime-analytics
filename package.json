{
  "name": "crime-analytics-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd crime-analytics-frontend && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('dist', '../dist', {recursive: true}); fs.cpSync('dist', '../', {recursive: true});\""
  }
}
